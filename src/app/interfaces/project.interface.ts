/*
import {IMain} from "./IMain";

export interface IProject extends IMain{
    _title: string;
    _link?: string;
    _image?: string;
    _owner?: string;
    _published?: boolean;
    _newProject?: boolean;

    title():string;
    title(v:string);
    link():string;
    link(v:string);

    image():string;
    image(v:string);
    owner():string;
    owner(v:string);
    published():boolean;
    published(v:boolean);
    newProject():boolean;
    newProject(v:boolean);

}*/
